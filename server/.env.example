# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=edu_system

# JWT 配置
JWT_SECRET=
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# 服务器配置
PORT=3001
NODE_ENV=development

# 前端地址（CORS）
FRONTEND_URL=http://localhost:3000
